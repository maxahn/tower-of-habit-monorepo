{
  "name": "tower-of-habit",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.21.0",
  "engines": {
    "node": ">=24"
  },
  "prepare": "husky",
  "scripts": {
    "build": "pnpm -r --filter './packages/**' build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "db:generate": "pnpm --filter @tower-of-habit/db generate",
    "db:migrate": "pnpm --filter @tower-of-habit/db migrate",
    "services:up": "ENV_FILE=.env.local docker compose --env-file .env.local up -d",
    "services:down": "ENV_FILE=.env.local docker compose down",
    "dev:up": "ENV_FILE=.env.local docker compose -f docker-compose.yml -f docker-compose.override.yml -f docker-compose.dev.yml --env-file .env.local up -d",
    "dev:down": "ENV_FILE=.env.local docker compose -f docker-compose.yml -f docker-compose.override.yml -f docker-compose.dev.yml --env-file .env.local down",
    "dev:worker": "ENV_FILE=.env.local docker compose -f docker-compose.yml -f docker-compose.dev.yml --env-file .env.local up -d postgres redis && pnpm --filter @tower-of-habit/worker dev",
    "dev:api": "ENV_FILE=.env.local docker compose -f docker-compose.yml -f docker-compose.dev.yml --env-file .env.local up -d postgres redis && pnpm --filter @tower-of-habit/api dev",
    "dev:mobile": "ENV_FILE=.env.local docker compose -f docker-compose.yml -f docker-compose.dev.yml --env-file .env.local up -d postgres redis && pnpm --filter @tower-of-habit/mobile dev",
    "services:up:staging": "ENV_FILE=.env.staging docker compose --env-file .env.staging up -d",
    "services:down:staging": "ENV_FILE=.env.staging docker compose --env-file .env.staging down",
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/config-conventional": "^21.2.2",
    "@typescript-eslint/eslint-plugin": "^8.68.0",
    "@typescript-eslint/parser": "^8.68.0",
    "commitlint": "^21.2.2",
    "eslint": "^10.8.1",
    "eslint-config-prettier": "^10.1.8",
    "husky": "^9.1.7",
    "lint-staged": "^17.4.1",
    "prettier": "^3.9.6",
    "typescript": "catalog:",
    "typescript-eslint": "^8.68.0"
  }
}
